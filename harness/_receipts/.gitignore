# Machine-generated evidence, regenerated by every ./verify run.
# Versioning it means the harness dirties the working tree each time it runs,
# and a harness that does that is one people stop running. CI keeps it as a
# build artifact instead (see the  job in .github/workflows/ci.yml).
*.json
